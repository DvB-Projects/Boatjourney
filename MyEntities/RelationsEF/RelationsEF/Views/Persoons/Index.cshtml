@model IEnumerable<RelationsEF.Models.Persoon>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GeboorteDatum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adres.Straat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adres.Bus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adres.Nummer)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GeboorteDatum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adres.Straat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adres.Bus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adres.Nummer)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
